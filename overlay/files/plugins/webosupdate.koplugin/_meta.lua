local _ = require("gettext")
return {
    fullname = _("webOS updates"),
    description = _([[Checks the webOS App Museum for newer versions of this KOReader port, and offers to install them via Preware.]]),
}
